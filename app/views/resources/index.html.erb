<section id="hero" class="main-section">
    <div class="row">
        <div class="columns xsmall-12 small-10 small-centered text-center">
            <h1 class="alpha">A crowdsourced repository of resources to assist you in helping with the recent Earthquake in Pakistan and Afghanistan.</h1>

            <a class="button turq show-how-it-works" href="javascript:void(0)">How does it work?</a>

        </div>
    </div>
</section>

<section class="how-it-works">
    <div class="row">
        <div class="columns xsmall-12 small-10 small-centered">
            <h5 class="epsilon">This website is designed to comunicate important information and raise contributions for disaster areas. The aim is to make it easier for people to contribute through donations (<a class="link" href="#donations">Donations section</a>) as well as provide news and resources to people helping on the ground (<a class="link" href="#news-and-resources">News and Resources section</a>). You can:</h5>
            <h5 class="epsilon text-left">1. Post a resource, news or donation campaign,</h5>
            <h5 class="epsilon">2. Or view existing resource, news or donation campaign. If you might know someone who can contribute, share the resource, news or campaign through twitter, facebook or sms.</h5>
            <h5 class="epsilon">Based on the impact of the post, we will move it into the Suggested section.</h5>
        </div>
    </div>
</section>

<section id="donations" class="main-section">
    <div class="row">
        <div class="columns xsmall-12">
            <h2 class="beta">Donations</h2>
        </div>
    </div>

    <div class="row sub-section">
        <div class="columns xsmall-12">
            <h4 class="delta">Need to Know</h4>
            <% if @donation_resources_need_to_know != [] %>
                <% @donation_resources_need_to_know.each do |resource| %>
                    <div class="single-resource">
                        <p><span class="title">Resource:</span> <%= resource.title %></p>
                        <p><span class="title">Website:</span> <%= link_to "#{resource.website_link}", resource.website_link, class: "link", target: "_blank" %></p>
                        <p><span class="title">Contact Number:</span> <%= resource.contact_num %></p>
                        <p><span class="title">Notes:</span> <%= resource.description %></p>
                        <%= link_to "Edit Resource", edit_resource_path(resource), class: "button orange" %>
                    </div>
                <% end %>
            <% end %>
        </div>
    </div>

    <div class="row sub-section">
        <div class="columns xsmall-12">
            <h4 class="delta">Suggested Resources</h4>
            <% if @donation_resources_suggested != [] %>
                <% @donation_resources_suggested.each do |resource| %>
                    <div class="single-resource">
                        <p><span class="title">Resource:</span> <%= resource.title %></p>
                        <p><span class="title">Website:</span> <%= link_to "#{resource.website_link}", resource.website_link, class: "link", target: "_blank" %></p>
                        <p><span class="title">Contact Number:</span> <%= resource.contact_num %></p>
                        <p><span class="title">Notes:</span> <%= resource.description %></p>
                    </div>
                <% end %>
            <% end %>
        </div>
    </div>

    <div class="row sub-section">
        <div class="columns xsmall-12">
            <h4 class="delta">Efforts on the Ground</h4>
            <% if @donation_resources_efforts_on_the_ground != [] %>
                <% @donation_resources_efforts_on_the_ground.each do |resource| %>
                    <div class="single-resource">
                        <p><span class="title">Resource:</span> <%= resource.title %></p>
                        <p><span class="title">Website:</span> <%= link_to "#{resource.website_link}", resource.website_link, class: "link", target: "_blank" %></p>
                        <p><span class="title">Contact Number:</span> <%= resource.contact_num %></p>
                        <p><span class="title">Notes:</span> <%= resource.description %></p>
                        <%= link_to "Edit Resource", edit_resource_path(resource), class: "button orange" %>
                    </div>
                <% end %>
            <% end %>
        </div>
    </div>
</section>

<section id="news-and-resources" class="main-section">
    <div class="row">
        <div class="columns xsmall-12">
            <h2 class="beta">News and Resources</h2>
        </div>
    </div>

    <div class="row sub-section">
        <div class="columns xsmall-12">
            <h4 class="delta">Need to Know</h4>
            <% if @other_resources_need_to_know != [] %>
                <% @other_resources_need_to_know.each do |resource| %>
                    <div class="single-resource">
                        <p><span class="title">Resource:</span> <%= resource.title %></p>
                        <p><span class="title">Website:</span> <%= link_to "#{resource.website_link}", resource.website_link, class: "link", target: "_blank" %></p>
                        <p><span class="title">Contact Number:</span> <%= resource.contact_num %></p>
                        <p><span class="title">Notes:</span> <%= resource.description %></p>
                        <%= link_to "Edit Resource", edit_resource_path(resource), class: "button orange" %>
                    </div>
                <% end %>
            <% end %>
        </div>
    </div>

    <div class="row sub-section">
        <div class="columns xsmall-12">
            <h4 class="delta">Suggested Resources</h4>
            <% if @other_resources_suggested != [] %>
                <% @other_resources_suggested.each do |resource| %>
                    <div class="single-resource">
                        <p><span class="title">Resource:</span> <%= resource.title %></p>
                        <p><span class="title">Website:</span> <%= link_to "#{resource.website_link}", resource.website_link, class: "link", target: "_blank" %></p>
                        <p><span class="title">Contact Number:</span> <%= resource.contact_num %></p>
                        <p><span class="title">Notes:</span> <%= resource.description %></p>
                    </div>
                <% end %>
            <% end %>
        </div>
    </div>

    <div class="row sub-section">
        <div class="columns xsmall-12">
            <h4 class="delta">Efforts on the Ground</h4>
            <% if @other_resources_efforts_on_the_ground != [] %>
                <% @other_resources_efforts_on_the_ground.each do |resource| %>
                    <div class="single-resource">
                        <p><span class="title">Resource:</span> <%= resource.title %></p>
                        <p><span class="title">Website:</span> <%= link_to "#{resource.website_link}", resource.website_link, class: "link", target: "_blank" %></p>
                        <p><span class="title">Contact Number:</span> <%= resource.contact_num %></p>
                        <p><span class="title">Notes:</span> <%= resource.description %></p>
                        <%= link_to "Edit Resource", edit_resource_path(resource), class: "button orange" %>
                    </div>
                <% end %>
            <% end %>
        </div>
    </div>
</section>